{
  "name": "@flamoral/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and interfaces for Flamoral",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "lint": "eslint src || true"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
