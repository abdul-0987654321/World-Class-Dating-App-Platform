{
  "name": "@connectsphere/shared",
  "version": "1.0.0",
  "description": "Shared packages for ConnectSphere (types, utils, constants, validators)",
  "private": true,
  "workspaces": [
    "types",
    "utils",
    "constants",
    "validators",
    "api-client"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -pt run build",
    "lint": "yarn workspaces foreach -pt run lint",
    "test": "yarn workspaces foreach -pt run test"
  }
}
