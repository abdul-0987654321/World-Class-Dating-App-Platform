apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chat-worker.fullname" . }}-config
  labels:
    {{- include "chat-worker.labels" . | nindent 4 }}
data:
  NODE_ENV: "production"
  WORKER_TYPE: "chat"
  LOG_LEVEL: "info"
