--- a/infrastructure/helm/flamoral/templates/deployment.yaml
+++ b/infrastructure/helm/flamoral/templates/deployment.yaml
@@ -47,6 +47,12 @@
           env:
             - name: NODE_ENV
               value: {{ .Values.global.environment | quote }}
             - name: PORT
               value: {{ .Values.apiGateway.service.port | quote }}
+          envFrom:
+            - configMapRef:
+                name: {{ $fullName }}-api-gateway-config
+            - secretRef:
+                name: {{ $fullName }}-secrets
+                optional: true
           livenessProbe:
             httpGet:
               path: /health
