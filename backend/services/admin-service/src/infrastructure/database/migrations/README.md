# Database Migrations

This directory contains database migration files for this service.

## Running Migrations
Migrations are run automatically on service startup via the database initialization module.

## Creating New Migrations
Create a new file with the naming convention: `YYYYMMDD_description.ts`
