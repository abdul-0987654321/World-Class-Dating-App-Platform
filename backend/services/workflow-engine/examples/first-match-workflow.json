{
  "name": "First Match Celebration",
  "description": "Congratulate users on their first match and encourage engagement",
  "trigger": {
    "type": "match_created",
    "config": {
      "description": "Triggered when user gets their first match"
    }
  },
  "conditions": [
    {
      "type": "match_count",
      "operator": "eq",
      "value": 1,
      "logicalOperator": "AND"
    }
  ],
  "actions": [
    {
      "type": "send_push",
      "config": {
        "title": "🎉 You've got your first match!",
        "body": "Say hi to {{match.firstName}} and start the conversation",
        "data": {
          "action": "navigate_to_match",
          "matchId": "{{match.id}}"
        }
      },
      "delay": 0,
      "retryOnFailure": true
    },
    {
      "type": "add_coins",
      "config": {
        "amount": 10,
        "reason": "first_match_bonus"
      },
      "delay": 1000,
      "retryOnFailure": true
    },
    {
      "type": "send_in_app_message",
      "config": {
        "title": "First Match Bonus!",
        "message": "We've added 10 free coins to your account. Use them to unlock premium features!",
        "type": "success",
        "actionUrl": "/store"
      },
      "delay": 2000,
      "retryOnFailure": false
    }
  ],
  "status": "active",
  "priority": 90,
  "tags": ["milestone", "engagement", "gamification"]
}
