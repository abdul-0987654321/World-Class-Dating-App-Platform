/**
 * Secure API Client - Re-export from api.client.ts
 * This module provides a secure interface with CSRF protection
 * and uses sessionStorage for token management
 */

export { apiClient, ApiError } from './api.client';
export { default } from './api.client';
